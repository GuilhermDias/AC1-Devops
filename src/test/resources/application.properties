# Testes nunca dependem de Docker nem de um Postgres no ar: rodam sempre no H2 em memoria.
# Este arquivo esta no classpath de teste e tem precedencia sobre src/main/resources.
spring.application.name=AC1-DevOps
spring.profiles.active=h2

spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.open-in-view=true
